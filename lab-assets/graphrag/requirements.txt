# Stage 3 GraphRAG lab — Python 相依
# 固定主要版本，避免 LightRAG API 變動導致範例失效（如需升級請同步檢查 adapter 的 init 寫法）
lightrag-hku==1.3.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
openai>=1.59.2,<2
python-dotenv==1.0.1
tenacity==9.0.0
# 課堂模式 B 用 Neo4j 圖儲存後端時需要（take-home 檔案式可略）
neo4j==5.27.0
# lightrag-hku==1.3.0 會 import 但未自動帶入的相依
pandas>=2.0
networkx>=3.0
tiktoken>=0.7
pipmaster>=0.2
numpy>=1.26,<2
nano-vectordb
